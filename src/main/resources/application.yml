spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  datasource:
     url: jdbc:postgresql://localhost:5432/schoollinkdb
     username: postgres
     password: Ricardo290&

logging:
  level:
    org:
      hibernate: 
        type: 
          descriptor:
            sql: trace